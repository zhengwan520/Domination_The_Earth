ItemEvents.tooltip(event => {
    event.addAdvanced('endrem:wither_eye', (item, advanced, text) => {
      if (!event.shift) {
        text.add(1, [Text.of('按住').gold(), Text.of('Shift ').yellow(), Text.of('查看获取方式').gold()])
      } else {
        text.add(1, Text.red('玩家击杀凋灵获得').bold(true))
    }
  })
  })

  ItemEvents.tooltip(event => {
    event.addAdvanced('endrem:old_eye', (item, advanced, text) => {
      if (!event.shift) {
        text.add(1, [Text.of('按住').gold(), Text.of('Shift ').yellow(), Text.of('查看获取方式').gold()])
      } else {
        text.add(1, Text.red('玩家击杀娜迦获得').bold(true))
    }
  })
  })

  ItemEvents.tooltip(event => {
    event.addAdvanced('endrem:cryptic_eye', (item, advanced, text) => {
      if (!event.shift) {
        text.add(1, [Text.of('按住').gold(), Text.of('Shift ').yellow(), Text.of('查看获取方式').gold()])
      } else {
        text.add(1, Text.red('玩家击杀九头蛇获得').bold(true))
    }
  })
  })


  ItemEvents.tooltip(event => {
    event.addAdvanced('endrem:evil_eye', (item, advanced, text) => {
      if (!event.shift) {
        text.add(1, [Text.of('按住').gold(), Text.of('Shift ').yellow(), Text.of('查看获取方式').gold()])
      } else {
        text.add(1, Text.red('玩家击杀暮初恶魂获得').bold(true))
    }
  })
  })


  ItemEvents.tooltip(event => {
    event.addAdvanced('endrem:undead_eye', (item, advanced, text) => {
      if (!event.shift) {
        text.add(1, [Text.of('按住').gold(), Text.of('Shift ').yellow(), Text.of('查看获取方式').gold()])
      } else {
        text.add(1, Text.red('玩家击杀幻影骑士获得').bold(true))
    }
  })
  })


  ItemEvents.tooltip(event => {
    event.addAdvanced('endrem:cold_eye', (item, advanced, text) => {
      if (!event.shift) {
        text.add(1, [Text.of('按住').gold(), Text.of('Shift ').yellow(), Text.of('查看获取方式').gold()])
      } else {
        text.add(1, Text.red('玩家击杀冰雪女王获得').bold(true))
    }
  })
  })

  ItemEvents.tooltip(event => {
    event.addAdvanced('endrem:black_eye', (item, advanced, text) => {
      if (!event.shift) {
        text.add(1, [Text.of('按住').gold(), Text.of('Shift ').yellow(), Text.of('查看获取方式').gold()])
      } else {
        text.add(1, Text.red('玩家击杀巫妖获得').bold(true))
    }
  })
  })

  ItemEvents.tooltip(event => {
    event.addAdvanced('endrem:exotic_eye', (item, advanced, text) => {
      if (!event.shift) {
        text.add(1, [Text.of('按住').gold(), Text.of('Shift ').yellow(), Text.of('查看获取方式').gold()])
      } else {
        text.add(1, Text.red('玩家击杀巨鳗获得').bold(true))
    }
  })
  })

  ItemEvents.tooltip(event => {
    event.addAdvanced('endrem:nether_eye', (item, advanced, text) => {
      if (!event.shift) {
        text.add(1, [Text.of('按住').gold(), Text.of('Shift ').yellow(), Text.of('查看获取方式').gold()])
      } else {
        text.add(1, Text.red('玩家击杀雪怪首领获得').bold(true))
    }
  })
  })


  ItemEvents.tooltip(event => {
    event.addAdvanced('endrem:rogue_eye', (item, advanced, text) => {
      if (!event.shift) {
        text.add(1, [Text.of('按住').gold(), Text.of('Shift ').yellow(), Text.of('查看获取方式').gold()])
      } else {
        text.add(1, Text.red('玩家击杀虚空之花获得').bold(true))
    }
  })
  })

  ItemEvents.tooltip(event => {
    event.addAdvanced('endrem:cursed_eye', (item, advanced, text) => {
      if (!event.shift) {
        text.add(1, [Text.of('按住').gold(), Text.of('Shift ').yellow(), Text.of('查看获取方式').gold()])
      } else {
        text.add(1, Text.red('玩家击杀下界铁掌获得').bold(true))
    }
  })
  })

  ItemEvents.tooltip(event => {
    event.addAdvanced('endrem:guardian_eye', (item, advanced, text) => {
      if (!event.shift) {
        text.add(1, [Text.of('按住').gold(), Text.of('Shift ').yellow(), Text.of('查看获取方式').gold()])
      } else {
        text.add(1, Text.red('玩家做出海神之怒获得').bold(true))
    }
  })
  })